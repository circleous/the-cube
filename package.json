{
  "name": "the-cube",
  "version": "1.0.0",
  "private": true,
  "description": "A Rubik's Cube game for the browser.",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "oxlint && oxfmt --check && vitest run"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "happy-dom": "^20.14.5",
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0",
    "sass": "^1.104.1",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
